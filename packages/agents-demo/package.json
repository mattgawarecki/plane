{
  "name": "@plane/agents-demo",
  "version": "1.3.1",
  "private": true,
  "description": "Demo-only tooling: seeds and drives a LOCAL Plane instance to showcase agentic activity. Never shipped.",
  "license": "AGPL-3.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check:lint": "oxlint --max-warnings=4 .",
    "check:types": "tsc --noEmit",
    "check:format": "oxfmt --check .",
    "fix:lint": "oxlint --fix .",
    "fix:format": "oxfmt .",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "@plane/agents": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@plane/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "tsx": "^4.19.2",
    "typescript": "catalog:",
    "vitest": "^4.0.8"
  }
}
